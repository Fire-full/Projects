Class inherited СИС2.БазовыйБланк "Табличный Документ" ;

InClass Public

Служебная    : TemplateSection;   --Кнопочная секция("Печать","Настройка Печати","Предварительный Просмотр", ...)
секПриемник  : TemplateSection;


  func Create :ТаблДокумент.ТабличныйБланк;
    Result  = inherited Create;

  end;

InObject Private

--------------------------------------------------------------------------------
--{ Обработчики событий бланка

  функ шаблон_ПриОтмене :Логическое;
    -- Вызывается при отмене изменений записи в бланке-редакторе
     
    Result = Истина; -- Разрешаем отмену записи
  конец;

  proc шаблон_ПриОткрытии(Create :Logical);
    inherited шаблон_ПриОткрытии(Create);
    -- Вызывается при открытия окна формы
    --   Create: True - форма открывается, False - форма восстанавливается при запуске сессии
    секПриемник = nil;
    Create      = True;
  end;

  функ шаблон_ПриЗаписи :Логическое;
    -- Вызывается при сохранении записи в бланке-редакторе
     
    Result = Истина; -- Продолжить сохранение записи
  конец;

  проц шаблон_ПриСчитывании;
    -- Вызывается при смене текущей записи в бланке-редакторе
     
  конец;

  func CellOnClick(Cell :TemplateCell; Action :Template.ClickTypes) :Logical;
    -- Обработка события нажатия в зависимости от типа
    -- Параметры:
    --   Cell: клетка шаблона
    --   Action: тип нажатия (SingleClick, DoubleClick, EnterPressed, ButtonPressed)
     
    Result = True; -- Разрешаем стандартную обработку
  end;

  proc BlankOnCreate(Context :Variant);
    -- Вызывается при создании объекта класса

  end;

  func ПолеЗаголовокОкнаФормы_ПриВыводе(Cell :TemplateCell; Value :Variant; Action :Template.OutputTypes; var Format :String) : Строка;


    Если Value="" Тогда
      
    Илсе;

    Result = inherited ПолеЗаголовокОкнаФормы_ПриВыводе(Cell,
                                                        Value,
                                                        Action,
                                                        Format);

    Если Value="" Тогда
      
    Илсе;

    -- Обработка вывода значения поля в зависимости от типа события
    -- Параметры:
    --   Cell: клетка шаблона
    --   Value: значение в поле клетки
    --   Action: тип вывода (Вывод,Вычисление,Копирование,Экспорт)
    --   Format: строка-формат вывода значения
     
   -- Result = Value; -- Что получили, то и выводим
  end;

--}

end